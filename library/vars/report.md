# Vars: report

+++
type = "vars"
namespace = "report"
+++

```toml
# Directory for report outputs (intended path; MVP prints content instead of writing files)
dir = "reports"

# Filenames for different report detail levels
brief_filename = "brief.md"
verbose_filename = "full.md"
```
