# Vars: filters

+++
type = "vars"
namespace = "filters"
+++

```toml
# Ignore hidden dotfiles by default
ignore_hidden = true

# Common ignore patterns (extendable)
ignore_patterns = [
  ".DS_Store",
  "Thumbs.db",
  "~*",
  "*.tmp",
  "*.swp"
]
```
